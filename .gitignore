# Python
__pycache__/
*.py[cod]
*$py.class

# 가상환경
recycling_env/
.venv/
venv/
env/

# 모델 가중치 / 내보내기 (대용량 — 저장소에 포함하지 않고 직접 배치)
*.pt
*.onnx
*.ncnn.*

# 변환 데이터셋 (대용량)
data/converted/
data/_temp_images/

# LiDAR 실측 로그 (lidar_range_probe.py 출력)
lidar_probe_log.csv

# IDE / 에디터
*.code-workspace
.vscode/
.idea/

# PlatformIO (펌웨어 빌드 산출물)
.pio/

# OS
Thumbs.db
.DS_Store

# 런타임 로그/캡처 (대용량 임시 산출물)
Log/
captures/

# KiCad 편집 히스토리(내장 git 저장소라 커밋 불가)
.history/
*.lck

# 학습 YOLO 작업본(자체 git 저장소 + 대용량 모델 -> 메인 저장소 제외, 모델은 직접 배치)
YOLO/
